# Version log

Bump `FW_VERSION` in `pumpsaver.ino` on every change pass. See `CLAUDE.md`.

| Version | Date | Flashed | Change |
|---------|------|---------|--------|
| 0.20.0 | 2026-09-08 | not yet | **Calibration screen, two sweep UI fixes, and the real E3 register map.** The sweep moves off System onto its own **`/cal`** screen with a nav tab. **Fix — the gpm column could not be typed into:** the 1 Hz poll rewrote the table's `innerHTML` unconditionally, destroying the input mid-keystroke. It now renders only when the measured data actually changes *and* nothing inside the table has focus; gpm and cavitation are excluded from that change signature because they are the operator's own edits coming back. **Fix — the step-through toggle flicked itself off:** it had no change handler, so nothing was ever sent to the board and the next poll read the stale value back over it. It now posts on change. **The manual arrived, and it corrected two guesses.** Motor output voltage is register **2014** and DC bus is **23** — the raw 9/10/11/15 logging added in 0.19.1 was guesswork and is gone. **Register 2006 is motor torque (±200.0 %)**, which `DESIGN_NOTES.md` called the highest-leverage open item on flow estimation: torque carries no magnetising offset, worth about ±4.5 gpm against current's ±11. Torque and power (reg 2004) are now captured per sweep step. New `MODBUS_E3.md` records the register map, control word, parameter formula and trip codes from the guide, **including the corrections**: registers 129/130 are *not* P-01/P-02 (the formula is documented for P-04 upward only, and the keypad reads P-02 = 18.0 against register 130's 3480); function 16 works on registers 1–4 only; P-12 = 3 was never at fault; and P-12 = 3 still requires the hardware enable on DI1. |
| 0.19.1 | 2026-09-08 | not yet | **Sweep: hand step-through, adjustable dwell, cavitation column, extra registers.** Dwell is operator-set (default 10 s) rather than fixed. **Step-through mode** holds the pump *at* each speed until Next is pressed — which is what reading a flow meter actually needs, since a 10 s window is not long enough to walk to a gauge; the loop is gpm → Next → gpm → Next. A **cavitating** checkbox per row records what the operator heard, which is the one input no instrument on this board can supply and the thing the cap table exists to avoid. A large red **STOP SWEEP** appears only while a run is live. Held steps are bounded at 10 minutes: a hand-stepped deadhead would otherwise run until somebody came back to the bench, which is a different risk from the 70 s already accepted. Also logs registers **9, 10, 11 and 15** raw. Invertek publishes that output volts and DC bus are on Modbus but not which register holds which — the manual says ask a sales partner — so they are exported as register numbers, not guessed labels, and the sweep itself identifies them: output voltage tracks speed, DC bus does not. Those reads are deliberately separate transactions from the status/Hz/amps read, so a rejected block cannot take the control feedback down with it. |
| 0.19.0 | 2026-09-08 | not yet | **Pump sweep — curve capture.** Steps the pump 30→60 Hz in 5 Hz increments, 10 s per step, averaging the settled last 2 s, and records commanded Hz, actual Hz (reg 7), pressure, raw AI counts, amps and heatsink temperature. Two runs: **deadhead** (valve shut) and **wide open**, with a hand-typed flow column on the open run — labelled `flow_gpm_manual` in the CSV because it is an operator reading off a meter, not something this board measured. The deadhead run is the point: it *measures* shutoff head against speed, and `shutoffPsiAt60` = 84 has been an unverified estimate feeding the cap table, the sleep thresholds and the whole envelope plot. Fitting P against speed² also yields suction pressure as the intercept (see `HANDOFF.md`). It drives the pump through **manual mode** rather than opening a second speed path, so the firmware still has exactly one route that bypasses the PI loop. Aborts above a settable pressure ceiling (**120 psi**, operator's call), on a drive trip, or on loss of comms — the last two are not safety padding: a step cannot be recorded from a drive that is not answering, so continuing would only log fiction. `/sweep` GET+POST, `/sweepcsv` download, `sweep dead\|open\|abort\|show` on the console, and a card on the **System** screen — deliberately not Pump, which is already the 41 KB page with an unexplained load failure. **Noted and overridden:** 7 × 10 s is 70 s of continuous no-flow running, against the 30–60 s guidance in `HANDOFF.md`; 5 s per step would collect the same data since pressure settles almost as fast as the drive ramps at zero flow. Operator accepted the risk deliberately, wanting the pressure tank time to react. |
| 0.18.1 | 2026-09-08 | not yet | **Fix: the charge began by commanding the pump slower.** Phase 1 completes with pressure at or above setpoint — reported from the bench as "reaches 51 at a 50 psi setpoint". Entering the charge raised `spActive` to `sp + sleepBoost` but left `spEff`, the ramped setpoint the PI actually chases, sitting at `sp`. So the first error of the charge was `50 − 51 = −1`: **negative**, commanding a speed *reduction*, and staying negative until the ramp climbed past the live reading — about 0.6 s at the default `sleepBoost/chargeRampS` of 1.67 psi/s. Charging is a request for more pressure and must never open by asking for less speed. `spEff` is now seeded from the live pressure at the 0→1 transition. Observed from the bench as "when charge kicks in it seems like the I gain resets" — nothing resets `pidI` there (only the enable edge and entering full sleep do); it was this ramp segment. |
| 0.18.0 | 2026-09-08 | not yet | **Command echo: read registers 1 and 2 back from the drive.** `pollDrive()` now reads our own command registers back and `/status` reports them per drive (`cmdW`, `refRaw`), with a live line under the manual slider on the Pump screen and in console `status`. This exists because the board commanded 4.5 Hz while the motor held 54 Hz, and *"what we believe we sent"* and *"what the drive is holding"* are different facts — the fault lives in the gap between them, and nothing in the firmware could see that gap. Deliberately on the **web** page: the serial console dies from VFD noise on the USB cable every time the motor runs, which is precisely when this reading matters, and WiFi survives it. The echo is labelled as a command echo and never substituted for a measurement — register 7 remains the only thing reported as actual motor speed. |
| 0.17.3 | 2026-09-08 | not yet | **Fix: `wr` used the wrong Modbus function code, and errors said nothing.** The E3 accepts function **16** (write multiple) for **registers 1–4 only**; every parameter write above that was answered with silence. `Rtu::writeSingle` (function **06**) already existed and was simply not used — `wr` now calls it, so the board can write drive parameters. `rd` and `wr` failures now print `rtu.errName()` (timeout / CRC / exception / bad frame) instead of a bare "no answer", which is the difference between "the drive refused this" and "nothing is on the wire". **Documented facts now on record**, from the Optidrive E3 advanced technical manual rather than inference: register 2 is *setpoint frequency ×10, range 0–5000* — so the existing `hz × 10` scaling is correct; P-12 = 3 and 4 are both valid Modbus network control modes, so control source was never the fault; and the parameter-to-register formula `128 + P` is documented **only for P-04..P-60**, meaning the earlier reading of registers 129/130 as P-01/P-02 is an extrapolation and not established — the keypad is authoritative for those. |
| 0.17.2 | 2026-09-08 | not yet | **`reset` on the console, and the trip code in `status`.** Every reflash silences Modbus for ~30 s, so the drive's own P-36 watchdog trips it — the watchdog doing exactly its job, but it leaves a trip that has to be cleared before anything will run, and the console had no way to clear one. `status` now prints the decoded trip code next to a tripped drive instead of leaving it packed in the high byte of the status word. **Correction to the 0.17.1 note:** the `heap` command added there measured 163,828 bytes largest free block against a 40,905-byte Pump page, so heap fragmentation was *not* why `/pump` failed to load. `send_P` is kept — it avoids a 40 KB heap copy per request and streams from flash, which is strictly better over the AP — but it is not established as the fix, and the real cause is still open. |
| 0.17.1 | 2026-09-08 | not yet | **Fix: `/pump` would not load.** Every page was served with `r->send(200, "text/html", PAGE_X)`, and passing a `const char*` there binds to the `const String&` overload — so the whole page is copied onto the heap as a String before a byte is sent, needing one **contiguous** block. Pump is 40.6 KB against 20–26 KB for every other screen, so once WiFi and AsyncTCP had fragmented the heap it was the only page that could not find its allocation: the small pages loaded, the big one silently failed. Now `send_P`, which streams straight out of flash and copies nothing. Applied to all five screens, not just Pump — the others were the same bug with more headroom, and Pump only crossed the line first. New `heap` console command reports free heap, **largest free block** (the number that actually decides this) and every page size, so the margin is measurable rather than guessed at. Not caused by the manual-mode UI added in 0.17.0; that added ~2 KB to a page which was already failing before it. |
| 0.17.0 | 2026-09-08 | not yet | **Manual speed mode — the slider is the speed command.** 0–60 Hz on the Pump screen with the PI loop, cavitation cap, sleep and staging all bypassed: `hzCmd` is whatever is dialled in. New `STATE_MANUAL`, `/man` endpoint, and `man off\|<hz>` on the console. Lives in `PumpIn` beside the override, so like `gEnable` it cannot be persisted — a reboot always comes back in automatic. **Deliberately not gated on `psiValid`:** everywhere else a dead transducer freezes the loop, because everywhere else the loop is steering on that measurement; manual is the mode you need precisely when the transducer is missing or lying, so the operator is the feedback path and both screens say so in a warning banner that outranks the pressure-invalid line. Switching into or out of manual drops the run demand — returning to automatic with a pump still turning would hand a live pump to a PI loop whose integrator was last set by hand; the integrator and setpoint ramp are seeded from the manual speed and current pressure on the way out. Unlike the override, the slider applies **while dragging** (debounced 150 ms): this is a hand throttle, and a throttle that only answers when you let go is not one. **Context — why this was needed:** a 10 Hz override test on the real E3 ran the motor at ~58 Hz. The probe added in 0.16.0 showed register 2 accepting and reading back our reference exactly, and the override clamping `hzCmd` to 10.0 for 40+ consecutive samples, so neither was at fault. A read of the drive's parameter block (found at register **128 + P**, anchored by 460 V / 9.5 A / 60 Hz at regs 135–137 and 5.00 s accel/decel at 131–132) showed **P-02 minimum speed = 3480** — RPM, and 3480 RPM is 58.0 Hz on a 2-pole motor. The drive was clamping our reference up to its own floor. P-12 reads 3, so control source was never the problem. Writing P-02 over Modbus was **refused** (no answer) — `Rtu` implements only function 0x10, and P-14 reads 201 rather than 101, so parameter writes need investigating before the board can commission a drive itself. |
| 0.16.0 | 2026-09-08 | not yet | **Raw Modbus probe on the serial console: `hold`, `rd`, `wr`.** Written to answer one question after a 10 Hz override test on a real E3 produced roughly 58–60 Hz at the motor: is register 2 actually the speed reference? The control tick rewrites registers 1–2 every 100 ms, so a hand-written value could not survive long enough to be read back — `hold on` stops the tick writing to drives (reads continue) so a probe is possible at all. `rd <addr> <reg> [n]` and `wr <addr> <reg> <v>` then read and write the map directly, with the control block entirely out of the path. Two guards: `wr` is refused unless the bus is held, and **refused outright for a non-zero value to register 1**, the command word — a raw console is for probing the map, not for starting motors; the run path stays `run`, which goes through the control block and its limits. Holding the bus means a running drive stops hearing from us and its own P-36 watchdog trips it, which is the safe direction. Not persisted — a probe, not a mode. **Also: a build guard for USB CDC On Boot.** Building from the IDE with that menu set to Disabled binds `Serial` to UART0 rather than `HWCDCSerial`/`USBSerial`, and the only symptom is `'class HardwareSerial' has no member named 'setTxTimeoutMs'`, which points nowhere near the cause. The worse outcome is the build that would succeed if someone deleted that call to make the error go away: console, CSV log and upload port all move to UART0 pins, so a board that flashes cleanly comes up mute on USB. A `#error` now names the menu option instead. `build.ps1` passes the whole FQBN and was never affected — this only bites in the IDE. |
| 0.15.0 | 2026-09-08 | not yet | **Serial bench console on the USB port.** `help`, `status`, `log on\|off`, `sim on\|off`, `scan`, `regs [addr]`, `ovr off\|<pct>`, `hz <hz>`, `run`, `stop`, `sta clear`, `reboot`. The web UI is still the real interface, but it lives on the board's own AP and a bench machine with one radio cannot join that AP without giving up the network it is already on — that blocked hardware work two sessions running. Worse in the field: when the configured station SSID is out of range the station retries forever and the softAP follows it around the channels, so the AP is hardest to join exactly when it is the only way in; `sta clear` is the way out without a laptop that can reach the page. Reads are non-blocking and the console is polled before the tick gate, so it answers at full speed and can never stall control — the rule `Serial.setTxTimeoutMs(0)` exists for. `log off` mutes the 10 Hz CSV so the console is readable. `regs` prints raw register values and nothing derived, because it is the tool you reach for when you do not yet trust the addressing and a decoded value would hide a wrong-slave answer. The simulation on/off safety sequence is factored into `simSwitch()` and shared by `/sim` and the console, the same way `cfgClamp()` is shared by `/set` and import, so the "drop enable and write stop/0 Hz before switching" rule cannot drift between the two entry points. |
| 0.14.0 | 2026-09-08 | **yes — 2026-09-08, bench** | **Selectable MQTT telemetry, ThingsBoard mode, and three data-honesty fixes.** Telemetry is now built group by group against a saved `pubMask` — pressure, speed, flow estimate, state, pumps, **motor amps**, **drive temps**, **raw AI counts**, status/trips, health — with a checkbox list on the Network screen and a live "N groups · ~B bytes every S s · ≈ MB/day" readout, because the first thing a cloud broker with a data allowance needs is a cost. Payload verified on hardware against a local broker: 314 B / 27 keys all-on, 97 B / 8 keys for a three-group selection, `{}` for an empty mask, valid JSON in every case. Mask bits the running build has no checkbox for are preserved through a save, so an older page cannot silently switch off a group a newer firmware added. **ThingsBoard mode:** publishes to the fixed `v1/devices/me/telemetry` and hides the base topic, since ThingsBoard routes on the access token in the MQTT username and ignores the topic — without this the broker connects, ACKs every message and charts nothing, with no error anywhere. **Fix — every board identified itself as `000000`:** `netDeviceId()` ran before `WiFi.softAP()` and `WiFi.macAddress()` returns six zero bytes until the WiFi stack is up. That string is the MQTT client id, the telemetry topic and the mDNS unit TXT, so two boards on one broker would have taken turns evicting each other forever, each looking healthy on its own screen. Now read from efuse with `esp_read_mac()`. **Fix — `/status` and MQTT published the SIMULATOR's flow unconditionally.** `plant.flowGPM` is only stepped while simulating, so on a real pump it was 0 at boot or frozen at the last simulated value, and it went into the historian looking exactly like a measurement. Now `flowEst`, from the affinity-law curve solved backwards on real pressure and speed; `flowSim` is kept separately for the sim panel. **Fix — `flowEst` under-reported by the pump count:** `flowAt()` is per pump, so with two staged it read 35 gpm while 70 gpm was flowing. **UI:** the demand-gpm slider moved directly under the operating-envelope plot, where you can watch the operating point move while dragging it. **Fix:** the Network page polled every 5 s and rewrote every input, so anything half-typed was reverted mid-edit; the poll now refreshes the status card only once the form is touched. |
| 0.13.0 | 2026-09-07 | **yes — 2026-09-07, bench** | **Fix: the control loop was stalling.** The per-tick CSV line is written from inside the control tick, and a USB CDC write **blocks** while the host is not draining the port. Measured with nothing reading serial: **401 ms average tick with one stall of 24 seconds**, against a designed 100 ms. After `Serial.setTxTimeoutMs(0)`: avg 100.0, min 100, max 100. This was the real cause of "simulation is not updating the values", and on a real skid an unplugged laptop would have done the same to a running pump. **Fix: no endpoint reporting live state sent a cache header** — clients were free to serve a stale reading, which on `/status` means a page showing a seconds-old pressure with no indication. All live endpoints now send `Cache-Control: no-store` via `sendNoCache()`, and every page fetch passes `cache:no-store`. **Simulated VFD registers:** the sim reported a status word of `0x0000` forever, so trips, trip codes and the ready bit were dead everywhere. It now *builds* a status word and decodes it through the same `decodeStatus()` the Modbus path uses; heatsink temperature (first-order thermal rise) and raw AI1 counts are modelled too, and `pollDrive()` reads regs 20 and 24 on the real path. **Fault injection** (`/fault`, simulation only): inject any trip code on either drive. A tripped lead drive also invalidates the transducer that lives on its AI1, so the loop freezes and holds the pumps — the real failure, simulated. Verified 4/4 deterministic. **New Sim screen** (`/sim`, 5th tab) — plant controls, 1-or-2 pump selector, per-drive register table and fault injection, moved off System. **Pump profiles** (`pump_profile.h`): a strict subset of settings describing the PUMP — curve, speed limits, cap table and new motor nameplate (hp / volts / FLA / rpm). Applying one deliberately leaves setpoint, sleep and staging alone, because those belong to the installation. Three built-in templates plus four user slots in NVS, with JSON export/import. `Settings` fields appended, so no tuning is lost. |
| 0.12.0 | 2026-09-07 | **yes — 2026-09-07, bench** | **Simulated plant controls sit with the Simulation toggle**, and a **gpm trace on the Home chart**. The plant panel (drive-by mode, header psi, demand gpm, time scale, tank gal/psi, live header/flow/speed readout) is now one shared `SIM_PANEL_HTML` / `SIM_PANEL_JS` macro pair in `ui_common.h`, rendered on both System (where you switch simulation on) and Pump (where you watch the operating point move on the envelope). One source, two pages — the duplication that would otherwise drift is gone. Home's trend chart gains a third trace: pressure autoscaled around setpoint, speed on a fixed 0–60 Hz, and **flow on its own autoscale with a 10 gpm floor**, so a 2 gpm trickle stays a visible line instead of being flattened by an earlier 90 gpm peak. Legend added under the chart. **Fix:** changing the pump count 2↔1 did nothing until the next reboot — `applySimDrives()` was gated on the simulation on/off edge, so a count change never reached `drv[].present` and staging kept being offered a lag pump that had been switched off. It now runs on every `/sim` post while simulating. **UX:** "simulation on, everything reads zero" is the expected state when the pump is stopped — a plant with no pump running really does sit at 0 psi — so the System status line and the Home banner now say exactly that instead of looking broken. |
| 0.11.0 | 2026-09-07 | **yes — 2026-09-07, bench** | **Simulation is a runtime mode, not a build.** Toggle on the System screen, with a 1-or-2-pump selector, so the real control block, sleep state machine and staging can be watched against a modelled plant on any board — speeds staying matched through a stage up and down, sleep and wake cycling, the operating point tracking across the envelope plot. The `#if SIM` / `#if !SIM` blocks are gone; both I/O paths compile into every build (37 % of a 3 MB partition, so it costs nothing). The compile-time `SIM` macro is now only the factory default for blank NVS. **Safety:** switching simulation on drops `gEnable` and writes stop / 0 Hz to any real drive on the bus *before* the switch — a drive is never left running against invented feedback, and never left to trip on its own P-36 watchdog, which would stop it as a fault someone has to go and reset. Switching it off rescans the bus. `simOn` persists (a demo board stays a demo board) but `gEnable` still does not, so a reboot in simulation comes back stopped. Config import/export deliberately does **not** carry `simOn` — restoring a config must not put a field board into simulation. `Settings` fields are appended, so no tuning is lost. **Fix:** the Network screen re-toasted the boot message (`AP FCW-PUMP at 192.168.4.1`) every 5 s forever, because `loadNet()` polls and `gNetLog` is state, not an event. The log line moved into the status card; only an actual change toasts, and never on first load. |
| 0.10.0 | 2026-09-07 | **yes — 2026-09-07, bench (SIM build)** | **Four-screen UI, built for a phone.** `/` Home (operator dashboard: big pressure readout, setpoint stepper + slider, live pressure/speed trend, per-drive cards), `/pump` (all tuning, envelope, diagnostics, override, sim), `/network` (identity, AP, station, static IP, MQTT), `/system` (CAN gateway, backup/restore, E3 keypad guide, controller info). Shared design system in `ui_common.h`; 44 px tap targets, real toggle switches in place of the 1/0 number boxes (still POSTed as 1/0, so every endpoint is unchanged), sticky save bars and toasts. **Fix — CAN form silently destroyed its own config:** `canStatusJSON()` did not return `capEnforce`, `driveAddr`, `hbMs`, `pdoMs`, `revision`, `serial` or `devType`, so the web form rendered them empty and "Save gateway" posted `""` for each, which `argF` reads as 0 — switching *off* P-01/P-02 enforcement and zeroing the identity. **New:** static IP / gateway / mask / DNS, configurable AP SSID and password (were compiled in), mDNS (`<host>.local`) with TXT records, `/sys` (uptime, reset reason, heap, PSRAM, partition use), `/reboot`. `/status` gains `up`. NetCfg fields are **appended**, so stored Wi-Fi credentials survive the update. `onNotFound` only does the captive-portal bounce for requests that arrived on the AP — a bad URL on the site network used to be redirected to an unreachable 192.168.4.1. **Partition scheme pinned to `app3M_fat9M_16MB`:** the board was on the 4 MB default table, using 1.25 MB of a 16 MB chip at 81 % full; now 3 MB app (37 %) with both OTA slots kept and 9.9 MB FATFS. **`SIM` is now set by `build.ps1 -Sim`** via a generated `sim_flag.h`, so the demo and real-drive binaries come from one commit. `build.ps1` probes for `arduino-cli` in both install locations and tests `$LASTEXITCODE` instead of `$?` (a compiler warning on stderr was reporting a clean build as failed). |
| 0.9.0 | 2026-09-07 | **yes — 2026-09-07, bench** | **Diagnostic max-Hz override + drive connection bubbles.** A 0–100 % slider in the Run card that, while engaged, **replaces** the cavitation cap and `maxHz` as the speed ceiling (100 % = 60 Hz, 0 % = stopped) and drops the minimum-speed floor to 0, so a pump can be walked all the way down on the bench. It lives in `PumpIn`, not `PumpCfg` — the same reasoning that keeps `gEnable` out of NVS: it defeats a protection, so a reboot must always come back with the cap enforcing. Sleep is suspended while engaged (the gates key off `hzCmd` and would otherwise sleep mid-test), the run bit drops at 0 % rather than holding the drive energised against a zero reference, and a red banner sits at the top of the page the whole time it is on. The ceiling is applied inside the PI clamp so anti-windup already accounts for it. `out.capHz` still reports the **real** cavitation cap while overridden — the override is not written back over a measurement. New `/ovr` endpoint; `ovr`/`ovrPct` in `/status`. Slider applies on release, not while dragging. Two green/red bubbles at the top of the page show per-drive link health, green only when the drive both answers reads **and** accepts writes. |
| 0.8.3 | 2026-09-07 | **yes — 2026-09-07, bench** | **First real-drive build — `SIM 0`.** Bench bring-up against an actual E3. **Write results are no longer discarded:** `commandDrive()` called `rtu.writeMultiple()` and threw the return away, so a drive that answered reads but refused writes (wrong P-12, locked parameter set) was indistinguishable from a healthy one. `Drive` gains `writeOK` / `writeFails`, the transition is announced on serial (`drive at 2: writes OK`), and both appear in `/status`. CSV log extended with the raw Modbus evidence per drive — `d1Hz,d1A,d1st,d1rd,d1wr` and the same for drive 2 — so a capture proves reads and writes rather than implying them. `gEnable` is false at boot and never persisted, so this build reads continuously and writes only stop / 0 Hz until someone enables it. |
| 0.8.2 | 2026-09-07 | not yet | **Build fix — the 0.6.0–0.8.1 tree never compiled.** `const char *FW_VERSION_STR = FW_VERSION;` sat above `#define FW_VERSION`, so the macro was undefined at the point it was expanded (*'FW_VERSION' was not declared in this scope*). The version block moved above the initialiser, with a comment saying why it has to stay there. Latent since 0.6.0 added `net.h` — no version between 0.6.0 and 0.8.1 was ever built, so it was never caught. Also on this pass: `build.ps1` now finds `arduino-cli` under `$env:LOCALAPPDATA\Programs\arduino-ide` (Arduino IDE 2.3.10 installs per-user, not to `C:\Program Files`), so neither path in it is machine-specific any more. |
| 0.8.1 | 2026-09-07 | not yet | **Drive setup card on the web page.** Step-by-step E3 keypad commissioning: factory reset (UP+DOWN+STOP → P-dEF), the comms parameters (P-14, P-12, P-36 address/baud/watchdog, P-31, P-16), motor nameplate data, RJ45 wiring, and what each scan result means. `build.ps1` config path now uses `$env:USERPROFILE`. |
| 0.8.0 | 2026-09-07 | not yet | **CAN gateway — the board impersonates the drive to the AIO skid PLC.** No CODESYS changes: the CR1082 keeps talking CANopen to node 1, and this board answers it, forwarding commands to the real E3 over Modbus. TX PDO1 is a verbatim copy of Modbus registers 6/7/8/24 — the E3 carries the same words in the same units on both links (9.2), so nothing is decoded and re-encoded. Drive parameters are *shadowed*: P-01/P-02 are stored, echoed on read-back, and enforced here by clamping the speed reference, which sidesteps the E3's undocumented Modbus parameter scaling. Standard objects are stored on write and returned on read, so the ifm CANopen Manager's startup configuration is agreed with rather than guessed at. If Modbus dies the board stops transmitting, so the PLC's own watchdogs trip. **Listen mode** captures a real bus to a frame log at `/can` — use it to read the drive's true 0x1018 identity before going live. |
| 0.7.0 | 2026-09-06 | not yet | **Config export / import.** `/export` downloads every setting as named JSON; `/import` accepts a pasted config. Named fields rather than the NVS blob, so a file written today still imports into later firmware — unknown keys ignored, missing keys left alone, everything re-clamped on the way in. **Passwords are never exported**; the network block carries SSID, broker and topic so a replacement unit is one paste from configured, then the two secrets get typed in on the board. Clamp limits moved into one `cfgClamp()` shared by `/set` and import so they cannot drift apart. |
| 0.6.0 | 2026-09-06 | not yet | **Station WiFi + MQTT telemetry.** Radio now `WIFI_AP_STA`: the FCW-PUMP config AP stays up permanently and the station side joins the site network, so the page is reachable whether or not that network works. Network settings live in their own NVS namespace (`psnet`), *not* the settings blob — a broker or SSID change no longer resets pump tuning. WiFi and MQTT run on a dedicated FreeRTOS task on core 0, so a blocking broker connect can never touch the control tick. Telemetry is publish-only with a retained online/offline last will; nothing is subscribed, so the network cannot command a pump. New `/net`, `/scan` endpoints and a Network card with an SSID picker. |
| 0.5.0 | 2026-09-06 | not yet | Sim can be driven by **pressure** as well as demand: pressure mode holds the header at a dialled-in psi (infinitely stiff source) so the loop reacts to it directly — sweeps the cap curve and flips the sleep gates without waiting on tank dynamics. |
| 0.4.0 | 2026-09-06 | not yet | Diagnostics dashboard (`/diag`) — PI terms, integrator pinning, anti-windup activity, every sleep and staging gate, and timer progress bars. **Fix:** a fixed `sleepHz` made sleep impossible above ~62 psi setpoint; the threshold now sits a margin above shutoff speed and tracks the setpoint (`sleepRelShutoff`). Added `chargeRampS` (3 s default) to ease into the charge. Flow-meter toggle exposed. Pump-curve help text plus a solver for free-flow from one measured point. Page moved to `page.h`. NVS magic → 0x50535635, settings reset. |
| 0.3.0 | 2026-09-05 | not yet | **Fix:** sim integrated the whole tick in one Euler step, so at time scale ×60 pressure overshot to 378 psi instead of settling at 60. Control and plant now co-simulate in ≤10 ms sub-steps. Sim controls are sliders that apply while dragging (debounced) and on release — no Apply button — each with a value box and adjustable range. Added `test/harness.cpp`, a desktop co-simulation of the real control block. |
| 0.2.0 | 2026-09-05 | not yet | Operating-envelope plot on the web page: cap curve, shutoff curve, true constant-flow contour at cavitation onset, live operating point and trail. New settings `qMax60` and `cavOnsetGPM` (display/reference only, not used by the control block). NVS magic bumped to 0x50535634 — **settings reset to defaults on first boot of this version**. |
| 0.1.0 | 2026-09-05 | not yet | First PumpSaver build. `FB_PumpControl` ported to `pump_control.h` as a pure block; `plant_sim.h` bench model; local Modbus RTU master with a 150 ms timeout; drive discovery over a range; web UI owns all parameters. SIM defaults to 1. |

## Versioning

- **patch** — fixes, tuning, comments
- **minor** — new feature, new setting, changed web API
- **major** — first field deployment, then breaking changes only

## Flash record

Record what actually went onto hardware, so a board in the field can be matched
to a commit. The running version is on the serial banner, the page header, and
`/status`.

| Version | Date | Board / skid | Notes |
|---------|------|--------------|-------|
| 0.10.0 (SIM) | 2026-09-07 | bench ESP32-S3, COM5 | Demo and UI-trial build: simulated plant, no drive driven. The real-drive binary is the SAME commit, built without `-Sim`. |
| 0.11.0 | 2026-09-07 | bench ESP32-S3, COM5 | Runtime simulation. Booted with simulation ON (blank-NVS default from the -Sim build); toggle it on the System screen. |
| 0.12.0 | 2026-09-07 | bench ESP32-S3, COM5 | Simulation controls on System, gpm trace on the Home chart. |
| 0.13.0 | 2026-09-07 | bench ESP32-S3, COM5 | Sim screen, VFD register simulation, fault injection, pump profiles. Control-tick stall fixed. |
